metadata:
  labels:
    app: demo-redis
    keyvalcluster: demo
  name: demo-headless
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: redis
    port: 6379
  - name: metrics
    port: 9121
  selector:
  - app=demo-redis
